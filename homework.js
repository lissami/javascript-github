const myCity = 'Kostroma'

const weather = {
    temperature: 3,
    windSpeed: 18
};

console.log('Мой город:', myCity);
console.log('Температура в городе:', weather.temperature);
console.log('Скорость ветра:', weather.windSpeed);

const result = ((20 + 5) * 3) // Это выражение

console.log(result) // Это показывает результат выражения выше